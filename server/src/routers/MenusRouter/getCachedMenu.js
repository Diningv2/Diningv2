export default async function getCachedMenu(location, todayDoc, tomorrowDoc) {
    // TODO: Sanat will implement everything from here onwards
    // Note: Should return undefined if the cache data is stale
    return undefined;
}
